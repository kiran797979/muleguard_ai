# MuleGuard AI — Python dependencies
# Install into a Python 3.11 (or 3.12) virtual environment:
#   python3.11 -m venv .venv && source .venv/bin/activate
#   python -m pip install --upgrade pip
#   python -m pip install -r requirements.txt
#
# Version floors are conservative; pip resolves compatible builds.

# --- Core data / numerics ---
pandas>=2.1,<3.0
numpy>=1.26,<2.2
scipy>=1.11

# --- Modelling ---
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.3
imbalanced-learn>=0.12

# --- Data dictionary (Description.xlsx) ---
# Required to read the F-code -> banking-variable mapping. Without it the
# pipeline still runs, but leak detection falls back to correlation alone and
# SHAP reasons stay as opaque F codes.
openpyxl>=3.1

# --- Fast intermediate storage ---
# Parquet keeps the 9k x 1.6k intermediates compact; utils falls back to CSV.
pyarrow>=14.0

# --- Explainability ---
shap>=0.44

# --- Graph intelligence ---
networkx>=3.2

# --- Visualisation ---
matplotlib>=3.8
seaborn>=0.13

# --- Utilities ---
joblib>=1.3
tqdm>=4.66

# --- Web layer (API + brutalist command-center UI) ---
# Serves the trained model and every pipeline report at http://127.0.0.1:8000.
# No Node, no build step: the UI is plain HTML/CSS/JS under app/static/.
fastapi>=0.110
uvicorn[standard]>=0.27
# Multipart form parsing, for uploading a dataset from the browser.
python-multipart>=0.0.9

# --- Optional: interactive EDA (comment out if not needed) ---
jupyterlab>=4.0
