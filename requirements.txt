# MuleGuard AI — Python dependencies
# Install into a Python 3.11 (or 3.12) virtual environment:
#   python3.11 -m venv .venv && source .venv/bin/activate
#   python -m pip install --upgrade pip
#   python -m pip install -r requirements.txt
#
# Version floors are conservative; pip resolves compatible builds.

# --- Core data / numerics ---
pandas>=2.1,<3.0
numpy>=1.26,<2.2
scipy>=1.11

# --- Modelling ---
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.3
imbalanced-learn>=0.12

# --- Explainability ---
shap>=0.44

# --- Graph intelligence ---
networkx>=3.2

# --- Visualisation ---
matplotlib>=3.8
seaborn>=0.13

# --- Utilities ---
joblib>=1.3
tqdm>=4.66

# --- Optional: interactive EDA (comment out if not needed) ---
jupyterlab>=4.0
